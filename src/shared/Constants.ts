export default class Constants {
	static STORAGE_KEY = {
		REFRESH_TOKEN: 'softflix.refreshToken',
		ACCESS_TOKEN: 'softflix.accessToken',
		EMAIL: 'softflix.email'
	};

	static NEXT_LOCALE = 'NEXT_LOCALE';
}
